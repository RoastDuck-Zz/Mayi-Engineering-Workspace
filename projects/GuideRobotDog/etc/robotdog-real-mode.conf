# Explicit real-mode deployment override. Startup does not connect the robot.
[Service]
ExecStart=
ExecStart=/bin/bash /home/guidedog/robotdog-control/scripts/run-server.sh --mode bpx --host 0.0.0.0 --port 8088 --robot-ip 10.21.20.1
